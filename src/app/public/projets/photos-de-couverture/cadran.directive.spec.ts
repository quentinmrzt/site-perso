import { CadranDirective } from './cadran.directive';

describe('CadranDirective', () => {
  it('should create an instance', () => {
    const directive = new CadranDirective(null);
    expect(directive).toBeTruthy();
  });
});
